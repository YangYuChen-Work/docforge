# 项目业务上下文

本文件是本项目的业务词汇表，不描述具体技术实现。任何需求、代码、接口或测试如果使用了下面的概念，都必须遵守这些定义。

## 核心概念

### 项目（Project）

一个产品研发或改型工作的业务容器。项目包含产品型号、项目编号、所处研发阶段、项目资料和待生成的项目文档。

项目不是文档。一个项目可以生成多份不同类型的文档。

### 产品（Product）

项目对应的产品或产品型号，例如 XCT80L7、XCA130、XGC260。产品是项目的业务对象，不等同于项目本身。

### 文档模板（Document Template）

规定文档用途、章节结构、表格结构、格式要求和生成规则的标准文档定义。

模板不是生成结果。模板可以被多个项目、多个生成任务复用。

### 来源资料（Source Material）

用于支撑文档生成的 Word、Excel、PDF 或其他项目资料。来源资料必须能够追溯到具体文件，必要时还要追溯到章节、表格或段落。

### 文档生成任务（Generation Task）

一次明确的“项目 + 模板 + 来源资料 → 文档初稿”的业务操作。任务包含输入资料、生成状态、处理结果、缺失资料和错误信息。

### 生成文档（Generated Document）

根据模板和来源资料产生的项目文档。生成文档可以被人工编辑、重新生成、评审、确认、导出和归档。

### 章节（Chapter）

文档模板中的一个业务内容单元。章节可以包含正文、表格、图片、引用和待补充项。章节是 AI 生成和人工确认的最小主要单位。

### 引用（Citation）

正文内容与来源资料之间的可追溯关系。引用至少记录来源文件，条件允许时记录来源章节、表格、段落或单元格。

### 资料匹配（Material Mapping）

将模板章节与来源资料建立关系的过程。匹配结果可以是已匹配、部分匹配、未匹配或冲突。

### 文档版本（Document Version）

生成文档在某个时间点的完整快照。AI 生成、人工修改、章节重新生成和确认都不能破坏历史版本。

### 待补充项（Missing Information）

模板要求或业务规则需要，但来源资料没有提供、资料内容不足或用户尚未确认的信息。待补充项不能被 AI 擅自编造。

### 场景1（Scenario 1）

本 POC 的真实业务场景：使用 `assets/scenario1/` 目录中的来源资料和目标 Word 模板，生成《XX产品开发立项暨设计和开发输入报告》类正式项目文档。

## 业务边界

- 当前 POC 的真实能力是场景1文档生成。
- 测试用例生成和产品模块/BOM选配属于后续业务方向，不是当前真实闭环。
- 当前为单用户本地 POC，不等同于企业级权限和多租户系统。
- AI 生成内容必须保留来源和人工确认入口。
